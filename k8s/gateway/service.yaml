apiVersion: v1
kind: Service

metadata:
  name: gateway-service
  namespace: gateway

  labels:
    app: gateway-service
    component: backend

spec:

  type: LoadBalancer

  selector:
    app: gateway-service

  ports:

    - name: http
      protocol: TCP
      port: 8080
      targetPort: http
