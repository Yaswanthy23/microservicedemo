apiVersion: v1
kind: Service

metadata:
  name: auth-service
  namespace: auth

  labels:
    app: auth-service
    component: backend

spec:

  type: ClusterIP

  selector:
    app: auth-service

  ports:

    - name: http

      protocol: TCP

      port: 8081

      targetPort: http
